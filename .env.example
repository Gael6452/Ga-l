# Optional: enables dynamic AI-generated recommendations via the Gemini API.
# Without it, AI Scout runs in demo mode using a curated, offline tool database.
#
# Get a key at https://aistudio.google.com/apikey then copy this file to `.env`.
VITE_GEMINI_API_KEY=
