{
  "name": "trioinclusion-app",
  "version": "1.0.0",
  "private": true,
  "description": "TrioInclusion app — Trouvez la meilleure IA pour votre métier.",
  "scripts": {
    "build": "node tools/build-www.js",
    "cap:add": "npm run build && cap add android",
    "cap:sync": "npm run build && cap sync android",
    "cap:open": "cap open android",
    "cap:run": "npm run build && cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
